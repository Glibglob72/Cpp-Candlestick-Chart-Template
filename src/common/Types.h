#pragma once

#include <cstdint>
#include <string>
#include <vector>

namespace Origin {
    using Price = double;
    using Volume = uint64_t;
    using Timestamp = int64_t;  // Unix timestamp in seconds
}
